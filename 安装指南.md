cd ~/TGH_ws

git status

git add .



git commit -m "算法001"

编译第三方包：
  mkdir build && cd build && cmake ..
  make
  sudo make install
编译完成后
source /opt/ros/noetic/setup.bash
source devel/setup.bash
运行仿真：
  roslaunch plan_manage kino_replan.launch B_Spline_LocalPlanner:=true sim_pose:=true


g


根据你上一轮对/home/hyh/mycar_ws的实际源码分析，现在开始实现。

总体原则：
1. 保留原TGH的拓扑路径生成算法。
2. 保留原HEC算法。
3. 保留原历史路径机制，优先扩展而不是替换。
4. 保留原B-spline局部优化器。
5. 不加入TRG terrain risk。
6. 不加入新的A*替代TGH。
7. 不进行与本功能无关的大规模重构。
8. 每一步修改都必须能够编译，并尽可能保持现有ROS topic/service/launch接口兼容。

实现目标：

第一步：Map Change Detection
在相邻规划周期之间识别变化区域 ΔM。
如果项目已有地图时间戳、ESDF update bounding box、changed voxel/cell机制，优先复用，不要重复扫描全图。
输出局部 dirty region。

第二步：Incremental Graph Update
根据 ΔM 找到受影响的拓扑节点和边：
V_dirty = {v | distance(v, ΔM) < r_update}
E_dirty = {e | e intersects ΔM}

只重新检查这些节点/边的碰撞有效性。

第三步：Invalid Repair
如果节点或边失效：
- 标记 invalid；
- 从有效图中临时移除；
- 找到依赖它的历史路径；
- 不要删除不相关图结构。

第四步：Historical Path Validation
对于历史路径 P：
如果 P ∩ ΔM = ∅：
    直接保留；
否则：
    只重新检查受影响片段。

将历史路径状态至少区分为：
VALID
AFFECTED
INVALID

第五步：Selective HEC
历史 VALID 路径不要重复执行完整HEC。
HEC主要用于：
- 新生成路径；
- 修复路径；
- topology可能发生变化的AFFECTED路径。

仍然必须保证最终候选集合中不存在原HEC所定义的重复拓扑路径。

第六步：Frontier Expansion
如果现有拓扑图无法覆盖新的自由空间/目标区域：
优先从图边缘有效节点继续扩展。
如果现有TGH图生成机制无法局部扩展，再设计最小范围的局部重新生成机制，不要强行套用TRG算法。

第七步：Global Graph Merge
将更新后的局部图合并回长期保存的global topology graph。
避免重复节点和重复边。
合并必须保持原TGH图结构的数据一致性。

第八步：保持现有输出
最终仍输出原TGH Guide Path，并继续交给原B-spline optimizer。

在实现过程中：
- 每完成一个功能块就编译；
- 如果使用catkin，使用项目原来的构建方式；
- 不要为了消除warning进行无关修改；
- 发现原代码设计与上述方案冲突时先解释，再采用最小侵入方案；
- 不允许用伪代码代替最终实现。

实现完成后请输出：
1. 修改了哪些文件；
2. 每个文件修改了什么；
3. 新增的数据结构；
4. 新增参数及默认值；
5. 新旧规划流程对比；
6. 如何验证incremental update真的生效；
7. 建议增加哪些ROS_DEBUG/统计量，包括：
   - graph_total_nodes
   - graph_updated_nodes
   - graph_updated_edges
   - reused_history_paths
   - invalidated_history_paths
   - HEC_check_count
   - full_rebuild_count
   - incremental_update_time
   - total_replanning_time
8. 编译结果及剩余问题。