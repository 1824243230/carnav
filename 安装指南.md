cd ~/TGH_ws

git status

git add .

git commit -m "ROS1能编译了"

git push

编译第三方包：
  mkdir build && cd build && cmake ..
  make
  sudo make install
编译完成后
source /opt/ros/noetic/setup.bash
source devel/setup.bash
运行仿真：
  roslaunch plan_manage kino_replan.launch B_Spline_LocalPlanner:=true sim_pose:=true