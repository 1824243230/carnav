cd ~/TGH_ws

git status

git add .

git commit -m "Modify local planner"

git push

编译第三方包：
  mkdir build && cd build && cmake ..
  make
  sudo make install
编译完成后
source /opt/ros/noetic/setup.bash
source devel/setup.bash