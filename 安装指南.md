cd ~/TGH_ws

git status

git add .

git commit -m "在Occupancy Map和ESDF基础上生成二维风险地图"

git push

编译第三方包：
  mkdir build && cd build && cmake ..
  make
  sudo make install
编译完成后
source /opt/ros/noetic/setup.bash
source devel/setup.bash
运行仿真：
  roslaunch plan_manage kino_replan.launch B_Spline_LocalPlanner:=true sim_pose:=true















  你是一名机器人运动规划算法工程师。

请基于当前TGH-Planner代码框架，实现一个新的算法：

Risk-aware Topology-Guided Hierarchical Planner (RTGH-Planner)

目标：
将TGH-Planner的Topology-guided global planning与trg-planner中Risk-aware Graph思想结合，用于2D未知非结构化环境路径规划。

不要重构整个工程。
保持ROS架构、接口、消息类型、原有B-spline优化框架不变。

请先分析当前代码结构：
1. global planner在哪里实现？
2. Topological Path Set在哪里维护？
3. Visibility-PRM在哪里生成？
4. Hybrid A*在哪里调用？
5. B-spline optimizer在哪里实现？
6. cost function在哪里定义？

输出：
- 文件结构分析
- 关键类说明
- 修改计划
- 需要新增的文件

不要直接修改代码。
基于当前TGH-Planner工程，实现Risk Map模块。

目标：
在Occupancy Map和ESDF基础上生成二维风险地图。

新增模块：

RiskMapManager

功能：

1. 输入：
- occupancy grid
- ESDF distance map

2. 输出：
risk value R(x,y)


风险模型：

R(x,y)=
lambda_d*R_distance
+
lambda_c*R_corridor
+
lambda_u*R_unknown


其中：

(1) obstacle distance risk:

R_distance =
1/(distance_to_obstacle + epsilon)


(2) unknown region risk:

unknown cell:
R_unknown=1

known free:
R_unknown=0


(3) corridor risk:

根据机器人当前位置周围自由空间计算通道宽度：

width = left_distance + right_distance

R_corridor =
1/(width - robot_width + epsilon)


要求：

- 使用C++
- 遵循ROS代码风格
- 不修改已有planner接口
- 提供参数yaml配置

新增参数：

risk_enable
lambda_distance
lambda_corridor
lambda_unknown
robot_width

请先生成代码结构，再实现。